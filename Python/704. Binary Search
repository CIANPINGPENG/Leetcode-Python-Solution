class Solution:
    def search(self, nums: List[int], target: int) -> int:
        
        if nums.count(target) >=1:
            i=nums.index(target)
            return i
        else:
            return -1
